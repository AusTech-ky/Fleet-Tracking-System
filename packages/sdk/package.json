{
  "name": "@fleet/sdk",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Typed TypeScript client for the FleetView API (GraphQL + auth)",
  "main": "src/index.ts",
  "scripts": {
    "test": "node --experimental-transform-types --test \"test/*.test.ts\""
  },
  "engines": { "node": ">=22" }
}
